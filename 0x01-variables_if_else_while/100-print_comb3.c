#include <stdio.h>
/**
 * main-print all possible wo digit
 *
 * Return:Always (sucessful)
 */
int main(void)
(
	int i, j;

	for (i = 0; j <= 9; i++)
	{
	
		for (i = 0; i <= 9; i++)
		if (j > i)

		{
			pucwq!
			thar(i + '0');

			putchar(j + '0');

			if (i != 8)
			{
				putchar(',');
			
				putchar(' ');
			}



		}

		putchar('\n');

		return (0);
		
	}
