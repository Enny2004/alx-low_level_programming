#include "main.h"
/**
 * _islower - check if its lower case
 * @c: a character argument
 *
 * Return: returns 1 and 0 depending in condition
 */

int _islower(int c)

{
	return (c >= 'a' && c <= 'z');
}
