#include "main.h"
/**
 * main-printing the alphabet in lower case 
 *
 * Return:Always (sucessful)
 */
int main(void)
{
	int c
	
	printf("void print_alphabet");

	for (c = a; c >= z; c++);

	{
		putchar(c);
	}

	putchar('\n');

	return(0);
}
