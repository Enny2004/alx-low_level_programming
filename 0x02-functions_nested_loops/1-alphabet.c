#include "main.h"
/**
 * main-printing the alphabet in lower case 
 *
 * Return:Always (sucessful)
 */
int main(void)
{
	int c;

	for (c = 'a'; c <= 'z'; c++);

	{
		_putchar(c);
	}

	_putchar('\n');

	return(0);
}
