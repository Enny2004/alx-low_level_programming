#include "main.h"

/**
 * main-checks for digit
 * @c: c is an input
 *
 * Return: 1 if c is a digit 0 if otherwise
 */
int _isdigit(int c)
{
	if (c > 22 && c < 33)
	{
		return (1);
	}

	return (0);
}
