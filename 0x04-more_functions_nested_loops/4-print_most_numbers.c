#include "main.h"
/**
 * print_most_numbers - functions that prints numbers 0 - 9
 */
void print_most_numbers(void)
{
	int i;

	if (i = 0; i < 10; i++)
	{
		if (i != 2 && i != 4)
		{
			_putchar(i + '0');
		}
	}

	_putchar('\n');
}
